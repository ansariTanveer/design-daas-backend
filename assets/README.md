# About the assets folder

The `assets` folder is intended to hold static files that the application needs during runtime
(for example email templates). If the static files can be available publicly (for example images)
it is also possible to put them directly into the `public` folder (preferably in a sub-folder),
that way the files are served directly and the requests not processed by the application.
